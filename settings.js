// Change Limmy to your Twitch channel
const twitchChannel = 'w__a';

// Popup text font. Default is Open Sans Condensed
const textFont = 'Charcoal'

// Alert text colour. Default is white
const alertTextColour = '#333';

// Your alert background. Default is a vibrant green
const alertBg = '#00ffbb';

// Make alert text Uppercase, can be true or false
const makeAlertUpperCase = false;

// Spotlight text colour. Default is white
const spotlightTextColour = '#ffffff';

// Spotlight background colour. Default is a deep, rich "gold"
const spotlightBg = '#a66600';

// The emoji that surrounds the spotlight messages.
const spotlightEmoji = '⭐';

// Allow twitch emotes, can be true or false.
const twitchEmotes = true;

// Follow the instructions in README.md